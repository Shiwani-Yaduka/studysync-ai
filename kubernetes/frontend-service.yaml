apiVersion: v1
kind: Service

metadata:
  name: frontend-service

spec:
  selector:
    app: studysync-frontend

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30001

  type: LoadBalancer